name: Build Windows x64
on:
  push:
    branches:
      - 'master'
    paths:
      - 'package.json'

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2

    - uses: actions/setup-node@v2
      with:
        node-version: '16'
    - run: npm install -g yarn
    - run: yarn install
    - run: yarn webbuild

    - uses: actions/setup-python@v3
      with:
        python-version: '3.x' # Version range or exact version of a Python version to use, using SemVer's version range syntax
        architecture: 'x64' # optional x64 or x86. Defaults to x64 if not specified
    - run: python -m pip install -r server/win64/requirements.txt
    - run: python -m pip install pyinstaller
    - run: pyinstaller -F server/win64/build.spec --distpath server/win64/dist/windows/
    - run: copy server/packet/win/* server/win64/dist/windows/

    - uses: actions/upload-artifact@v2
      with:
        name: vTablet_windows_x64
        path: server/win64/dist/windows # or path/to/artifact

